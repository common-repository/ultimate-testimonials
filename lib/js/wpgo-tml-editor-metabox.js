jQuery( document ).ready( function($) {

    // This runs on the TML post editor

    // passed in from PHP
    //var override_global_tml = customizer_preview_tml_js.wpgo_override_glb_tml_tmp;

    //$( "#wpgo_override_glb_tml_template" ).parent().prependTo("#pageparentdiv .inside");
    //$( "#pageparentdiv .inside" ).prepend("<div style='margin-top:12px;'></div>");

    // initially show the template drop down?
    //if( override_global_tml === '1' ) {
    //    $( ".post-attributes-label-wrapper, #page_template" ).show();
    //}

    //$( "#pageparentdiv" ).show();


    //$( "#wpgo_override_glb_tml_template" ).click(function() {
    //    $( ".post-attributes-label-wrapper, #page_template" ).toggle();
    //});
});